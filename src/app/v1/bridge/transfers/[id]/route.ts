import { NextResponse } from "next/server"; import { authenticatedUser } from "@/lib/server/auth"; import { bridgeRequest } from "@/lib/server/bridge"; import { apiError } from "@/lib/server/responses";
export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!(await authenticatedUser(request))) return apiError("UNAUTHORIZED", "Authentication required", 401); const { id } = await params; const result = await bridgeRequest(`/transfers/${encodeURIComponent(id)}`, request); return NextResponse.json(result.data, { status: result.status }); }
